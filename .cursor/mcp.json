{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "Файлы PhantomProxy: Go, YAML, HTML-шаблоны, docs/"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "description": "HTTP: management API :8081, Prometheus :9090/metrics, fallback upstream"
    }
  }
}
